const Api = {
    zones: {
        get: () => {
            return new Promise(() => {})
        },

        getSingle() {
            return new Promise(() => {})
        },

        add() {
            return new Promise(() => {})
        },

        update() {
            return new Promise(() => {})
        },

        delete() {
            return new Promise(() => {})
        },
    }
}

export default Api;
